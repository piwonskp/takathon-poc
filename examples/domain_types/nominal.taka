from enum import Enum


Color = Enum('Color', ('BLUE', 'CYAN', 'GREEN', 'YELLOW', 'RED'))

test is_red(color):
     domain any of(Color.BLUE, Color.CYAN, Color.GREEN, Color.YELLOW):
          results False
     domain Color.RED: results True
def:
  return color == Color.RED
